# Project Rules - Git Workflow

## Commit & Push Policy
- **CRITICAL**: Never execute `git commit` or `git push` directly without asking the user for explicit permission first.
- Always present the staged changes or proposed commit messages to the user and wait for their confirmation before making any Git commits or pushing code to GitHub.
